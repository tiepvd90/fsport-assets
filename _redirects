/feed-sitemap.xml  /feed-sitemap  200

# Dynamic product pages use a dedicated namespace so this rule can never
# intercept existing /pickleball, /giadung, /html or asset URLs.
/p/:slug  /dynamic-product.html  200
